const carController = require("./car");
const authController = require("./auth");
const categoryController = require("./category");

// Exporting the controllers using object shorthand notation
module.exports = {
  carController,
  authController,
  categoryController,
};
